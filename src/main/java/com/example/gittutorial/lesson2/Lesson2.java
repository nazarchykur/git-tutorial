package com.example.gittutorial.lesson2;

import lombok.Getter;
import lombok.RequiredArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@RequiredArgsConstructor
public class Lesson2 {
    private String field1;
    
    private String field2;
    
    /*
        studying add / commit, commit --amend (with/without rewriting msg) / status
        
        add some changes then add it to staging area
        remove some changes and then add it to staging area
        rewrite msg in commit message section using Intellij / console
        
     */
}
